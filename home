from ipaddress import *
k = 0
nets = ip_network('158.132.161.128/255.255.255.128', 0)

for net in nets:
    b = [bin(int(x))[2:].zfill(8) for x in str(net).split('.')]
    s = ''.join(b)
    if s[len(s) - 1] == 1:
        k += 1
print(k)
